#include "header.h"

void	saveTheSequoia(struct s_node **root) {
	struct s_node	*head;

	if (!root || !*root)
		return ;
	head = *root;
	if (head->left && head->left->value > head->value) {
		tmp = head;

	}
}
